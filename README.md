## Установка и Запуск

Скрипт позволяет с нужной частотой по кадрам нарезать видео 
В папке frames уже лежат готовые фоточки (так оно и должно выглядеть)

### 1. Клонирование репы и запуск

Сначала клонируйте репозиторий с помощью Git:

```bash
git clone "наш урлик"

cd project

pip install -r requirements.txt

python3 extract_frames.py